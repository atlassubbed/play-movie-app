import { describe, it } from "mocha"
import { expect } from "chai"
// if we test this, we can import from ../src/* and it will be transpiled before getting tested

// default placeholder test
describe("client", function(){
  it("is currently not tested... add some tests!", function(){
    
  })
})
